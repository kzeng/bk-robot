# M3 方案: 按距离停拍巡检落地方案

## 1. 方案介绍

M3 方案用于替代当前“预先在地图系统中大量打点, 然后按点位逐点移动, 到点停稳后拍照”的实施方式。

当前方案的主要问题不是机器人无法完成移动和拍照, 而是工程实施成本高:

- 每条巡检路线都需要人工预打大量地图点位。
- 点位数量一多, 地图维护、校准、复核成本很高。
- 现场环境稍有调整, 可能就需要重新打点。
- 真正花费的大量时间不在运行阶段, 而在实施阶段。

M3 的核心思想是:

- 仅保留必要的起点 A 和终点 B。
- 机器人从 A 到 B 时, 不再依赖中间大量预置点位。
- 系统先向思岚请求 A 到 B 的实际规划路径。
- 按配置的距离间隔, 例如默认每 0.5 米, 在路径上自动生成停拍点。
- 机器人按这些自动生成的停拍点分段移动, 每段到点后停稳, 等待 3 秒, 执行拍照, 再继续前进。

这不是“边走边拍”, 而是“沿路径按距离自动停拍”。

该方案的业务目标是:

- 降低工程实施对地图打点的依赖。
- 保证图像采集位置分布均匀。
- 保证拍照时机器人静止, 提高清晰度。
- 让现场可通过参数配置停拍间距, 适配不同项目。

## 2. 与当前方案的差异

当前方案:

- 任务中保存的是一串 marker。
- 后端按 marker 顺序调用移动。
- 到达 marker 后停稳拍照。
- 中间每一个拍照点都依赖预先打点。

M3 方案:

- 任务只需要表达起点 A 和终点 B, 或者当前位姿到终点 B。
- 中间停拍点不再人工维护。
- 中间停拍点由系统按路径距离自动插入。
- 每个停拍点都仍然遵循“到点, 停稳, 拍照”的闭环。

M3 替代的是“中间巡检点的人工打点方式”, 不是替代机器人自主导航本身。

## 3. 适用范围与边界

适用场景:

- 固定通道巡检。
- 长走廊或货架侧巡检。
- 只需要 A 和 B 明确, 中间不需要人工定义业务语义点位。
- 对拍照清晰度和空间采样均匀性有要求。

不适合直接替代的场景:

- 中间某些位置带有明确业务语义, 例如必须在电梯口、门口、工位口停留。
- 中间某些位置需要精确朝向控制, 且该朝向不是路径自然朝向。
- 多楼层跨层任务中, 中间要绑定 POI、门禁、电梯逻辑。

建议原则:

- 对“纯巡检采样路线”, 优先用 M3。
- 对“业务点位强相关路线”, 保留原 marker 方案或采用混合方案。

## 4. 配置设计

M3 应做成可配置方案, 默认值可满足大多数项目, 但不应写死。

### 4.1 推荐配置项

```ini
PHOTO_DISTANCE_MODE_ENABLED=true
PHOTO_DISTANCE_INTERVAL_M=0.5
PHOTO_STOP_DURATION_S=3
PHOTO_STOP_SETTLE_SPEED_THRESHOLD=0.02
PHOTO_SEGMENT_SPEED_RATIO=0.6
PHOTO_DISTANCE_MIN_INTERVAL_M=0.3
PHOTO_DISTANCE_MAX_INTERVAL_M=2.0
PHOTO_DISTANCE_REPLAN_ONCE_PER_SEGMENT=true
PHOTO_DISTANCE_MAX_SEGMENT_RETRY=3
```

### 4.2 配置项说明

| 配置项 | 默认值 | 含义 |
|---|---:|---|
| PHOTO_DISTANCE_MODE_ENABLED | true | 是否启用按距离停拍模式 |
| PHOTO_DISTANCE_INTERVAL_M | 0.5 | 停拍间距, 单位米 |
| PHOTO_STOP_DURATION_S | 3 | 到点停稳后的等待时长, 单位秒 |
| PHOTO_STOP_SETTLE_SPEED_THRESHOLD | 0.02 | 认为机器人已停稳的线速度阈值, 单位 m/s |
| PHOTO_SEGMENT_SPEED_RATIO | 0.6 | 每个分段导航的速度比例 |
| PHOTO_DISTANCE_MIN_INTERVAL_M | 0.3 | 允许配置的最小停拍间距 |
| PHOTO_DISTANCE_MAX_INTERVAL_M | 2.0 | 允许配置的最大停拍间距 |
| PHOTO_DISTANCE_REPLAN_ONCE_PER_SEGMENT | true | 每段开始前是否重新搜路一次 |
| PHOTO_DISTANCE_MAX_SEGMENT_RETRY | 3 | 单段导航最大重试次数 |

### 4.3 工程建议

- 0.5 米适合作为默认值。
- 0.3 到 0.5 米适合精细巡检。
- 0.6 到 1.0 米适合常规巡检。
- 大于 1.0 米更偏效率优先。

## 5. 数据模型与任务定义建议

当前任务模型在 [app/models.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/models.py#L4) 中只有 `marker`、`action`、`description` 等基础字段, 更适合“按点位任务”。

M3 的任务表达建议至少包含以下信息:

- 起点: `start_marker` 或 `use_current_pose`
- 终点: `end_marker`
- 模式: `distance_photo`
- 停拍间距: `distance_interval_m`
- 停留时长: `stop_duration_s`
- 分段速度比: `segment_speed_ratio`

如果短期内不改数据库结构, 可以采用过渡方式:

- 沿用现有 `Task.description` 保存 JSON 扩展参数。
- `Task.marker` 中仅存 `A,B` 两个点位。
- `action` 使用一个新的动作类型, 例如 `2` 表示按距离停拍。

长期建议是增加专门字段, 避免参数解析散落在业务代码中。

## 6. 涉及的思岚接口

M3 的核心不是依赖大量 POI, 而是依赖思岚的路径规划和动作控制接口。

### 6.1 获取当前位置

接口:

- `GET /api/core/slam/v1/localization/pose`

用途:

- 当任务从机器人当前位置开始时, 获取 A 点实时位姿。
- 用于运行中记录路径起点或异常恢复时确认当前位置。

Swagger 位置:

- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L1736)

### 6.2 搜索 A 到 B 的可行路径

接口:

- `POST /api/core/motion/v1/:search_path`

用途:

- 请求思岚返回从当前起点到目标点的路径点列表。
- 上层根据返回的路径点累计长度。
- 按 `PHOTO_DISTANCE_INTERVAL_M` 自动插入停拍点。

Swagger 位置:

- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L1610)

### 6.3 创建分段导航动作

接口:

- `POST /api/core/motion/v1/actions`

动作类型建议:

- `slamtec.agent.actions.MoveToAction`

用途:

- 将每个自动生成的停拍点作为一个独立的短距离导航段。
- 每段到点后完成闭环, 再发下一段。

原因:

- 比一次性下发整条连续轨迹更容易保证“到点停稳后再拍”。
- 更容易做单段失败重试与异常恢复。

Swagger 位置:

- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L1411)
- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L6666)
- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L6583)

### 6.4 查询动作状态

接口:

- `GET /api/core/motion/v1/actions/{action_id}`

用途:

- 查询当前分段导航是否完成。
- 判断当前段是否成功、失败、取消、暂停。

Swagger 位置:

- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L1493)

### 6.5 获取当前速度

接口:

- `GET /api/core/motion/v1/speed`

用途:

- 判断机器人是否真正停稳。
- 不建议仅依赖 action 完成状态直接拍照。
- 建议在 `vx`、`vy`、`omega` 均接近 0 时才进入拍照阶段。

Swagger 位置:

- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L1572)

### 6.6 获取当前动作剩余路径点

接口:

- `GET /api/core/motion/v1/path`

用途:

- 当机器人绕障或重规划后, 可读取当前剩余路径。
- 可用于诊断和高级版本的动态重分段。

Swagger 位置:

- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L1548)

### 6.7 可选接口: 全局速度与策略

接口:

- `PUT /api/core/system/v1/parameter`
- `PUT /api/core/motion/v1/strategies/:current`

用途:

- 在整个停拍任务前切到更平稳的运动模式。
- 调整全局最大速度或单段速度比例。

Swagger 位置:

- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L297)
- [docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/docs/robot-chassis/Slamware-RESTful-API-swagger-conf.json#L1657)

## 7. 关键闭环流程

M3 的关键不在“插点”, 而在完整闭环。

### 7.1 主流程

1. 读取任务配置。
2. 校验 `distance_interval_m` 是否在允许范围内。
3. 解析起点 A 和终点 B。
4. 若 A 为实时起点, 读取机器人当前位置。
5. 调用思岚搜路接口, 获取 A 到 B 的路径点。
6. 对路径点按累计距离进行插值, 生成停拍点序列。
7. 逐段执行:
   - 导航到下一个停拍点
   - 轮询动作完成状态
   - 轮询当前速度直至停稳
   - 等待配置的停留时长
   - 执行相机拍照
   - 记录日志、文件路径、耗时
8. 全部分段完成后, 将任务置为成功。
9. 任一分段失败超过阈值, 任务失败并落日志。

### 7.2 停稳判定

建议停稳条件为:

- 动作状态为完成, 或到点成功。
- 当前线速度满足 `abs(vx) < threshold`。
- 如果设备返回 `vy`, 同时满足 `abs(vy) < threshold`。
- 角速度满足 `abs(omega) < angle_threshold`。
- 连续 2 到 3 次采样都满足停稳阈值再进入拍照。

### 7.3 重规划策略

M3 方案有两种实施强度:

#### 版本 A: 一次搜路, 全程切点

- 任务开始时搜一次 A 到 B 的路径。
- 一次性生成全部停拍点。
- 实现简单, 适合环境较稳定的场景。

#### 版本 B: 每段重新搜路

- 每到一个停拍点后, 重新以当前位置到终点 B 搜路。
- 再计算下一个 `distance_interval_m` 停拍点。
- 对动态障碍和绕障更稳。

工程上建议先落地版本 A, 再根据现场稳定性演进到版本 B。

## 8. 与当前工程的结合点

### 8.1 当前移动控制入口

当前工程中的思岚适配器位于 [app/robot_control.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/robot_control.py#L9)。

当前已有能力:

- 创建动作 `create_action()` [app/robot_control.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/robot_control.py#L131)
- 按 marker 移动 `move_to_marker()` [app/robot_control.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/robot_control.py#L144)
- 取消当前动作 `cancel_move()` [app/robot_control.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/robot_control.py#L159)
- 查询动作状态 `get_action_status()` [app/robot_control.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/robot_control.py#L211)

M3 需要新增的适配层能力建议:

- `get_current_pose()`
- `search_path(target)`
- `get_current_speed()`
- `move_to_pose(target_pose, speed_ratio=None)`

这些能力本质上仍然是对现有 `_request()` 的薄封装。

### 8.2 当前任务主循环入口

当前任务执行主循环位于 [app/routes.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/routes.py#L1044) 的 `async_run_task()`。

当前照片任务逻辑特点:

- 从 `marker_list` 顺序取点。
- 每个点调用 `move_to_marker()`。
- 到点后固定等待 2 秒。
- 调用 `take_photo_all_cameras()` 拍照。

相关位置:

- 任务入口 [app/routes.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/routes.py#L1044)
- 照片任务分支 [app/routes.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/routes.py#L1162)
- 等待移动完成 [app/routes.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/routes.py#L989)
- 拍照调用 [app/routes.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/routes.py#L1209)

M3 的落地点建议:

- 新增一个任务分支, 不再从 `marker_list` 遍历所有中间点。
- 只解析起点 A 和终点 B。
- 生成自动停拍点列表后循环执行。

### 8.3 当前相机拍照入口

当前多相机抓图实现位于 [app/opencv_control.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/opencv_control.py#L177) 的 `take_photo_all_cameras()`。

M3 不需要改相机底层抓图逻辑, 但建议补充拍照元数据:

- segment_index
- planned_distance_m
- actual_pose_x
- actual_pose_y
- actual_yaw
- task_mode=M3

这样后续更容易追踪每张图片对应的停拍位置。

## 9. 关键代码实现

本节描述的是当前工程中的建议实现结构, 不是要求一次性大改。

### 9.1 适配器层建议

建议在 [app/robot_control.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/robot_control.py) 增加以下方法:

```python
def get_current_pose(self):
    return self._request("GET", "/api/core/slam/v1/localization/pose")


def search_path(self, target):
    return self._request(
        "POST",
        "/api/core/motion/v1/:search_path",
        json={"target": target}
    )


def get_current_speed(self):
    return self._request("GET", "/api/core/motion/v1/speed")


def move_to_pose(self, pose, speed_ratio=None):
    options = {
        "target": {
            "x": float(pose["x"]),
            "y": float(pose["y"]),
            "z": 0,
        },
        "move_options": {
            "mode": 0,
        },
    }
    if speed_ratio is not None:
        options["move_options"]["speed_ratio"] = speed_ratio
    return self.create_action("slamtec.agent.actions.MoveToAction", options)
```

说明:

- `move_to_marker()` 适合旧方案。
- `move_to_pose()` 是 M3 的核心入口。
- `search_path()` 用于替代人工中间点。

### 9.2 路径长度累计与停拍点插值

建议新增纯算法函数, 独立于 Flask 路由和思岚请求层。

```python
import math


def segment_length(p1, p2):
    return math.hypot(float(p2[0]) - float(p1[0]), float(p2[1]) - float(p1[1]))


def build_distance_stop_points(path_points, interval_m):
    if not path_points or len(path_points) < 2:
        return []

    result = []
    next_distance = interval_m
    traveled = 0.0

    for i in range(1, len(path_points)):
        p1 = path_points[i - 1]
        p2 = path_points[i]
        seg_len = segment_length(p1, p2)

        while traveled + seg_len >= next_distance:
            ratio = (next_distance - traveled) / seg_len
            x = p1[0] + (p2[0] - p1[0]) * ratio
            y = p1[1] + (p2[1] - p1[1]) * ratio
            result.append({
                "x": x,
                "y": y,
                "planned_distance_m": round(next_distance, 3),
            })
            next_distance += interval_m

        traveled += seg_len

    return result
```

说明:

- 思岚路径返回的是路径点列表。
- M3 的核心计算不是复杂导航算法, 而是“路径累计长度 + 线性插值”。

### 9.3 停稳闭环

建议新增停稳等待函数, 不要仅依赖 `wait_for_robot_move()`。

```python
import time


def wait_for_robot_settle(robot_control, speed_threshold=0.02, stable_count=3, timeout=10):
    deadline = time.time() + timeout
    success_count = 0

    while time.time() < deadline:
        speed = robot_control.get_current_speed()
        vx = abs(float(speed.get("vx", 0) or 0))
        vy = abs(float(speed.get("vy", 0) or 0))
        omega = abs(float(speed.get("omega", 0) or 0))

        if vx < speed_threshold and vy < speed_threshold and omega < speed_threshold:
            success_count += 1
            if success_count >= stable_count:
                return True
        else:
            success_count = 0

        time.sleep(0.2)

    return False
```

说明:

- 当前工程在拍照前仅固定 `sleep(2)`。
- M3 应改为“状态 + 速度”的真实停稳闭环。

### 9.4 M3 任务主循环伪代码

建议在 [app/routes.py](c:/Users/zen82746.LI/Desktop/tmp33/bk-robot/app/routes.py) 的任务执行逻辑中新增一条分支:

```python
def run_distance_photo_task(app, task, task_log):
    start_target = resolve_start_pose_or_marker(task)
    end_target = resolve_end_pose_or_marker(task)
    config = resolve_distance_photo_config(task)

    path_points = app.robot_control.search_path(end_target)
    stop_points = build_distance_stop_points(path_points, config.distance_interval_m)

    for index, stop_point in enumerate(stop_points, start=1):
        move_result = app.robot_control.move_to_pose(
            stop_point,
            speed_ratio=config.segment_speed_ratio,
        )
        action_id = move_result["results"]["task_id"]

        move_ok = wait_for_robot_move(
            app.robot_control,
            target_marker=f"segment-{index}",
            expected_task_id=action_id,
        )
        if not move_ok:
            raise Exception(f"segment {index} move failed")

        settle_ok = wait_for_robot_settle(
            app.robot_control,
            speed_threshold=config.stop_settle_speed_threshold,
        )
        if not settle_ok:
            raise Exception(f"segment {index} settle failed")

        time.sleep(config.stop_duration_s)

        photo_result = app.camera_control.take_photo_all_cameras(
            position_info=f"M3-{index}",
            timestamp=str(int(time.time())),
        )
        save_task_log_segment_result(task_log, index, stop_point, photo_result)

    final_move = app.robot_control.move_to_marker(end_target)
    return final_move
```

说明:

- 这里的示例重点在闭环结构。
- 实际实现时, `wait_for_robot_move()` 需要支持基于 pose 目标而不是 marker 名称判断成功。

### 9.5 当前位置与终点表达

M3 推荐支持两种任务输入:

#### 模式 A: 当前位姿 -> 终点 B

- 起点不需要预打点。
- 任务开始时读取当前位置。
- 适合人工临时发起巡检。

#### 模式 B: 起点 A -> 终点 B

- A 和 B 只保留少量必要点位。
- 适合标准化路线巡检。

建议默认优先支持模式 B, 因为路线复现性更强。

## 10. 异常处理策略

### 10.1 单段移动失败

- 重试当前段, 不直接重试整个任务。
- 超过 `PHOTO_DISTANCE_MAX_SEGMENT_RETRY` 后任务失败。

### 10.2 动态障碍导致路径变化

- 简版方案: 当前段失败后重新搜路一次。
- 进阶方案: 每段开始前都重新搜路。

### 10.3 到点但未停稳

- 进入停稳等待逻辑。
- 超时则允许重试拍照前等待一次。

### 10.4 拍照失败

- 不建议整任务立刻失败。
- 建议记录为 `PARTIAL` 或保留失败段日志。

## 11. 日志与可观测性建议

M3 方案需要比当前 marker 方案更细的日志, 否则现场不容易排查。

建议每个停拍段记录:

- segment_index
- planned_distance_m
- stop_point_x
- stop_point_y
- action_id
- move_elapsed_ms
- settle_elapsed_ms
- photo_status
- saved_file_count

建议在任务总日志中补充:

- total_stop_count
- configured_interval_m
- configured_stop_duration_s
- total_move_time_s
- total_stop_time_s
- total_photo_count

## 12. 分阶段实施建议

### 第一阶段

- 保留当前 marker 任务体系不动。
- 新增一种任务模式: M3 按距离停拍。
- 支持 A、B 两点输入。
- 支持默认 0.5 米停拍。

### 第二阶段

- 增加设置页配置项。
- 增加任务日志中的 segment 维度信息。
- 增加单段重试与失败分析。

### 第三阶段

- 支持按当前位姿起点运行。
- 支持每段重搜路。
- 支持轨迹可视化和停拍点回放。

## 13. 结论

M3 方案的本质价值不在导航能力增强, 而在工程实施方式优化。

它用“路径规划 + 按距离自动插点 + 分段停稳拍照”替代“人工预打大量中间点位”。

对于当前项目, 这意味着:

- 能明显降低实施阶段地图打点工作量。
- 能保持拍照位置均匀和结果可复现。
- 能继续复用现有思岚 REST 适配层和相机抓图能力。
- 风险主要集中在任务闭环和异常处理, 而不是底层接口能力不足。

如果按最小改动路径推进, 优先级建议如下:

1. 在思岚适配层补齐 `search_path`、`get_current_speed`、`move_to_pose`。
2. 为任务执行新增 M3 分支, 不影响旧任务。
3. 先采用“一次搜路, 全程切点”的简版。
4. 待现场验证稳定后, 再升级到“每段重搜路”。
